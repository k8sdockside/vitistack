{
    // Type checking only: esbuild bundles (`k8sdockside-plugin build`), and
    // `tsc --noEmit` makes sure what it bundles is sound. The base config
    // types `k8sdockside` and `window.k8sdockside` in every page.
    "extends": "@k8sdockside/plugin-sdk/tsconfig.json",
    "include": ["src"]
}
